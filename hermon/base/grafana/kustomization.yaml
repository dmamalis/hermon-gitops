apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - dashboard-json-configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
  - name: grafana-datasource-config
    files:
      - assets/provisioning/datasources/hermon-timescale.yaml

  - name: grafana-dashboard-provider-config
    files:
      - assets/provisioning/dashboards/hermon-dashboards.yaml
